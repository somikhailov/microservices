build_vue-micro:
  extends: .registry_common
  stage: build
  script:
    - docker build ./src/vue-micro -t $CI_REGISTRY_USER/vue-micro:$CI_COMMIT_TAG --build-arg API=$VUE_ROOT_API
    - docker push $CI_REGISTRY_USER/vue-micro:$CI_COMMIT_TAG
  only:
  - tags