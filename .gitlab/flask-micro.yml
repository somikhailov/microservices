build_flask-micro:
  extends: .registry_common
  stage: build
  script:
    - docker build ./src/flask-micro -t $CI_REGISTRY_USER/flask-micro:$CI_COMMIT_TAG
    - docker push $CI_REGISTRY_USER/flask-micro:$CI_COMMIT_TAG
  only:
  - tags